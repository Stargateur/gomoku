to install boost download the last version here http://www.boost.org/users/download/

and extract the zip in C:

next open the files and exec bootstrap.bat

open a cmd and go in this file and exec b2.exe -j 8 address-model=64 install